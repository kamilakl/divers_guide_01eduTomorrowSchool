# who-are-you (Кто ты?)
---

Твоя задача — создать Shell-скрипт who-are-you.sh, который будет обращаться к файлу JSON, извлекать из него имя пользователя по определенному ID и выводить только это имя.

---

Твоя задача — создать Shell-скрипт who-are-you.sh, который будет обращаться к файлу JSON, извлекать из него имя пользователя по определенному ID и выводить только это имя.

## Инструкции к заданию

"You just woke up in a dark alley... You can not remember who you are... The only thought that comes to your mind is a tag that says: Subject ID: 70"

Создай файл who-are-you.sh, который напомнит тебе, кто ты, показав только твое имя.

* Где искать: https://01.tomorrow-school.ai/assets/superhero/all.json

* Что использовать: curl и jq

* Вывод должен быть в точности как в примере, но с ожидаемым именем.

### Ожидаемый результат

При запуске скрипта:
```bash
$ ./who-are-you.sh | cat -e
name<EOT>
```
---

## 1. get-ready (Подготовка)

### 🛠️ Необходимые действия

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego` | Все задания должны быть здесь |
| **Проверить инструменты** | Убедиться, что установлены `curl` и `jq` | `curl` для скачивания, `jq` для обработки JSON |
| **Создать файл скрипта** | Создать пустой файл с именем **`who-are-you.sh`** | Здесь будет код твоего решения |

### 💻 Команды и объяснение

1. **Создание файла:**
    ```bash
    touch who-are-you.sh
    ```

---

## 📝 2. set (Написание скрипта)

Скрипт должен использовать `curl` для получения данных и `jq` для их фильтрации.

### 🎯 Ожидаемый результат

Скрипт должен вывести **только имя** и ничего больше, например: `Groot`.

### 💻 Команды и объяснение

Открой файл **`who-are-you.sh`** и добавь следующее содержимое:

```bash
#!/bin/bash
# 1. curl -s: Скачивает JSON тихо.
# 2. jq '.[] | select(.id == 70) | .name': Фильтрует массив ([]) по ID 70 и извлекает имя.
# 3. tr -d '"': Удаляет кавычки, чтобы вывод соответствовал требованию.

curl -s [https://01.tomorrow-school.ai/assets/superhero/all.json](https://01.tomorrow-school.ai/assets/superhero/all.json) | \
jq '.[] | select(.id == 70) | .name' | \
tr -d '"'
